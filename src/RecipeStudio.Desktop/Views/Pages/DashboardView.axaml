<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:RecipeStudio.Desktop.ViewModels"
             x:Class="RecipeStudio.Desktop.Views.Pages.DashboardView">

  <ScrollViewer>
    <StackPanel Margin="22" Spacing="16">
      <Grid ColumnDefinitions="*,Auto,Auto,Auto" Margin="0,6,0,0">
        <StackPanel>
          <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="∿" Classes="heading-icon" />
            <TextBlock Text="ПАНЕЛЬ УПРАВЛЕНИЯ" Classes="h1 dashboard-title" />
          </StackPanel>
          <TextBlock Text="Система готова к работе. Выберите рецепт для начала." Classes="muted" Margin="0,6,0,0" />
        </StackPanel>

        <Button Grid.Column="1" Command="{Binding NewRecipeCommand}" Classes="accent big" Margin="0,0,10,0">+ СОЗДАТЬ</Button>
        <Button Grid.Column="2" Command="{Binding ImportRecipeCommand}" Classes="icon-btn" Margin="0,0,10,0" ToolTip.Tip="Импорт точек из Excel/CSV">📊</Button>
        <Button Grid.Column="3" Command="{Binding RefreshCommand}" Classes="icon-btn">⟳</Button>
      </Grid>

      <Border Height="1" Background="#222633" />

      <Grid ColumnDefinitions="*,*,*">
        <Border Grid.Column="0" Classes="stat-card">
          <Grid ColumnDefinitions="*,Auto">
            <StackPanel Spacing="6">
              <TextBlock Text="ВСЕГО РЕЦЕПТОВ" Classes="muted stat-title" />
              <TextBlock Text="{Binding Recipes.Count}" FontSize="38" FontWeight="Bold" />
              <TextBlock Text="● Актуально" Foreground="#4ADE80" Classes="tiny" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="◍" Classes="stat-icon" Foreground="#1D4D8D" />
          </Grid>
        </Border>

        <Border Grid.Column="1" Classes="stat-card" Margin="14,0,0,0">
          <Grid ColumnDefinitions="*,Auto">
            <StackPanel Spacing="6">
              <TextBlock Text="СИСТЕМА" Classes="muted stat-title" />
              <TextBlock Text="ОНЛАЙН" FontSize="30" FontWeight="Bold" Foreground="#34D399" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="▣" Classes="stat-icon" Foreground="#166534" />
          </Grid>
        </Border>

        <Border Grid.Column="2" Classes="stat-card" Margin="14,0,0,0">
          <Grid ColumnDefinitions="*,Auto">
            <StackPanel Spacing="6">
              <TextBlock Text="ПОСЛЕДНЯЯ АКТИВНОСТЬ" Classes="muted stat-title" />
              <TextBlock Text="Сегодня" FontSize="30" FontWeight="Bold" />
              <TextBlock Text="Пользователь: Admin" Classes="muted tiny" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="◷" Classes="stat-icon" Foreground="#A16207" />
          </Grid>
        </Border>
      </Grid>

      <StackPanel Spacing="10" Margin="0,8,0,0">
        <TextBlock Text="ДОСТУПНЫЕ РЕЦЕПТЫ" Classes="section-label" />
        <Border Height="1" Background="#222633" />

        <ItemsControl x:Name="RecipeCards" ItemsSource="{Binding DashboardTiles}">
          <ItemsControl.Styles>
            <Style Selector="#RecipeCards > ContentPresenter">
              <Setter Property="Margin" Value="0,0,14,14" />
            </Style>
          </ItemsControl.Styles>

          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <WrapPanel />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>

          <ItemsControl.DataTemplates>
            <DataTemplate DataType="vm:RecipeCardViewModel">
              <Border Classes="recipe-card" Width="294" Height="190" PointerPressed="RecipeCard_PointerPressed" Tag="{Binding}" Cursor="Hand">
                <Grid RowDefinitions="104,*" ClipToBounds="True">
                  <Border Background="#1F2330" BorderBrush="#2A2E3F" BorderThickness="0,0,0,1">
                    <TextBlock Text="🗎" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="34" Foreground="#64748B" />
                  </Border>

                  <Grid Grid.Row="1" Margin="14,10,14,10" ColumnDefinitions="*,Auto">
                    <StackPanel Grid.Column="0" Spacing="7" VerticalAlignment="Center">
                      <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="185" />
                      <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                        <TextBlock Text="●" Foreground="#9CA3AF" Classes="tiny" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding PointCount, StringFormat='{}{0} точек'}" Classes="muted tiny" VerticalAlignment="Center" />
                      </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="9" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,0,4">
                      <Button Command="{Binding OpenCommand}" Classes="icon action edit" ToolTip.Tip="Редактировать">✎</Button>
                      <Button Classes="icon action delete" Tag="{Binding}" Click="DeleteRecipe_Click" ToolTip.Tip="Удалить">🗑</Button>
                    </StackPanel>
                  </Grid>
                </Grid>
              </Border>
            </DataTemplate>

            <DataTemplate DataType="vm:CreateRecipeTileViewModel">
              <Button Width="294" Height="190" Command="{Binding CreateCommand}" Classes="create-recipe">+</Button>
            </DataTemplate>
          </ItemsControl.DataTemplates>
        </ItemsControl>
      </StackPanel>
    </StackPanel>
  </ScrollViewer>
</UserControl>
