<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="RecipeStudio.Desktop.Views.Pages.DashboardView">

  <ScrollViewer>
    <StackPanel Margin="22" Spacing="16">
      <Grid ColumnDefinitions="*,Auto,Auto" Margin="0,6,0,0">
        <StackPanel>
          <TextBlock Text="ПАНЕЛЬ УПРАВЛЕНИЯ" Classes="h1 dashboard-title" />
          <TextBlock Text="Система готова к работе. Выберите рецепт для начала." Classes="muted" Margin="0,6,0,0" />
        </StackPanel>

        <Button Grid.Column="1" Command="{Binding RefreshCommand}" Classes="icon-btn" Margin="0,0,10,0">⟳</Button>
        <Button Grid.Column="2" Command="{Binding NewRecipeCommand}" Classes="accent big">+ НОВЫЙ РЕЦЕПТ</Button>
      </Grid>

      <Border Height="1" Background="#222633" />

      <Grid ColumnDefinitions="*,*,*">
        <Border Grid.Column="0" Classes="stat-card">
          <Grid ColumnDefinitions="*,Auto">
            <StackPanel Spacing="6">
              <TextBlock Text="ВСЕГО РЕЦЕПТОВ" Classes="muted stat-title" />
              <TextBlock Text="{Binding Recipes.Count}" FontSize="38" FontWeight="Bold" />
              <TextBlock Text="● Актуально" Foreground="#4ADE80" Classes="tiny" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="◍" Classes="stat-icon" Foreground="#1D4D8D" />
          </Grid>
        </Border>

        <Border Grid.Column="1" Classes="stat-card" Margin="14,0,0,0">
          <Grid ColumnDefinitions="*,Auto">
            <StackPanel Spacing="6">
              <TextBlock Text="СИСТЕМА" Classes="muted stat-title" />
              <TextBlock Text="ОНЛАЙН" FontSize="30" FontWeight="Bold" Foreground="#34D399" />
              <TextBlock Text="Версия: v0.1.0-alpha" Classes="muted tiny" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="▣" Classes="stat-icon" Foreground="#166534" />
          </Grid>
        </Border>

        <Border Grid.Column="2" Classes="stat-card" Margin="14,0,0,0">
          <Grid ColumnDefinitions="*,Auto">
            <StackPanel Spacing="6">
              <TextBlock Text="ПОСЛЕДНЯЯ АКТИВНОСТЬ" Classes="muted stat-title" />
              <TextBlock Text="Сегодня" FontSize="30" FontWeight="Bold" />
              <TextBlock Text="Пользователь: Admin" Classes="muted tiny" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="◷" Classes="stat-icon" Foreground="#A16207" />
          </Grid>
        </Border>
      </Grid>

      <StackPanel Spacing="10" Margin="0,8,0,0">
        <TextBlock Text="ДОСТУПНЫЕ РЕЦЕПТЫ" Classes="section-label" />
        <Border Height="1" Background="#222633" />

        <Grid ColumnDefinitions="*,280" Margin="0,2,0,0">
          <ItemsControl x:Name="RecipeCards" ItemsSource="{Binding Recipes}">
            <ItemsControl.Styles>
              <Style Selector="#RecipeCards > ContentPresenter">
                <Setter Property="Margin" Value="0,0,14,14" />
              </Style>
            </ItemsControl.Styles>

            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <WrapPanel />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Border Classes="recipe-card" Width="420" Height="190">
                  <Grid RowDefinitions="104,*" ClipToBounds="True">
                    <Border Background="#1F2330" BorderBrush="#2A2E3F" BorderThickness="0,0,0,1">
                      <TextBlock Text="🗎" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="34" Foreground="#64748B" />
                    </Border>

                    <Grid Grid.Row="1" Margin="14,10,14,12" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                      <StackPanel Grid.Row="0" Grid.Column="0" Spacing="7">
                        <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="240" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                          <Border Background="#232938" BorderBrush="#353D53" BorderThickness="1" CornerRadius="4" Padding="6,2">
                            <TextBlock Text="CSV" Classes="tiny" />
                          </Border>
                          <TextBlock Text="{Binding PointCount, StringFormat='~{0} точек'}" Classes="muted tiny" VerticalAlignment="Center" />
                        </StackPanel>
                      </StackPanel>

                      <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                        <Button Command="{Binding DeleteCommand}" Classes="danger">Удалить</Button>
                        <Button Command="{Binding OpenCommand}" Classes="accent">Открыть</Button>
                      </StackPanel>
                    </Grid>
                  </Grid>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>

          <Button Grid.Column="1" Command="{Binding NewRecipeCommand}" Classes="create-recipe" Margin="14,0,0,0">+</Button>
        </Grid>
      </StackPanel>
    </StackPanel>
  </ScrollViewer>
</UserControl>
